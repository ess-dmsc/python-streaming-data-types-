-r requirements.txt
black==22.3.0    # Pinned to match pre-commit config
flake8==6.0.0    # Pinned to match pre-commit config
isort==5.12.0    # Pinned to match pre-commit configblack
flake8
pre-commit
pytest
tox
twine
wheel
